const config = {
  reaktor_api_url: 'https://bad-api-assignment.reaktor.com/v2',
  product_url: 'products',
  availability_url: 'availability',
  cache_duration_seconds: 300,
}

module.exports = config