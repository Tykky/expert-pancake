{
  "name": "expert-pancake",
  "scripts": {
    "heroku-prebuild": "cd frontend && npm install && cd ../backend && npm install && npm run build",
    "install": "npm run heroku-prebuild",
    "start": "cd backend && npm start",
    "dev-backend": "cd backend && npm run dev",
    "dev-frontend": "cd frontend && npm start"
  }
}
